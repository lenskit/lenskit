# FIXME Re-enable this setting when rating builders work
# builder: org.lenskit.data.ratings.RatingBuilder
attributes:
  user: long
  item: long
  rating: double
  timestamp: long
columns: [user, item, rating, timestamp]