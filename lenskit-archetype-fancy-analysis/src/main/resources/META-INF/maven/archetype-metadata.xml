<?xml version="1.0" encoding="UTF-8"?>
<archetype-descriptor name="fancy-analysis">
  <id>lenskit-archetype-fancy-analysis</id>
  
  <!-- These resources will be copied out of the archetype-resources
       directory into the newly created target project. -->
  <fileSets>
    <fileSet filtered="true" packaged="true">
      <directory>src/test/java</directory>
      <includes>
        <include>**/*.java</include>
      </includes>
    </fileSet>
    <fileSet filtered="true" packaged="true">
      <directory>src/main/java</directory>
      <includes>
	<include>**/*.java</include>
      </includes>
    </fileSet>

    <!-- These are the files we want at the top-level that actually do
	 the script work for running the eval and analysis. -->
    <fileSet filtered="true">
      <directory></directory>
      <includes>
	<include>data/get-data.xml</include>
	<include>eval/eval.groovy</include>
	<include>analysis/chart.R</include>
      </includes>
    </fileSet>
  </fileSets>
</archetype-descriptor>

